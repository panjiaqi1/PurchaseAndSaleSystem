<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.GoodExtendedFieldMapper">

    <resultMap id="goodExtendedFieldResultMap" type="com.example.demo.entity.GoodExtendedField">
        <id property="id" column="id"/>
        <result property="value" column="value"/>
        <association property="good" javaType="Good">
            <id property="id" column="good_id"/>
            <result property="name" column="good_name"/>
        </association>
        <association property="extendedField" javaType="ExtendedField">
            <id property="id" column="extended_field_id"/>
            <result property="name" column="extended_field_name"/>
        </association>
    </resultMap>

    <select id="findAll" resultMap="goodExtendedFieldResultMap" parameterType="org.springframework.data.domain.Pageable"
            resultType="GoodExtendedField">
        SELECT good_extended_field.id,
        good_extended_field.`value`,
        good_extended_field.extended_field_id,
        good_extended_field.good_id
        FROM good_extended_field
        <if test="pageable != null">

            <bind name="size" value="pageable.getPageSize()"/>
            <if test="pageable.sort != null">
                <foreach collection="pageable.sort" item="sort" index="index">
                    <if test="index == 0">
                        order by
                    </if>
                    <if test="index > 0">
                        ,
                    </if>
                    ${sort.property} ${sort.direction.name}
                </foreach>
            </if>
            limit #{pageable.offset} , ${size}
        </if>
    </select>

    <select id="findById" resultMap="goodExtendedFieldResultMap">
    SELECT good_extended_field.id,
        good_extended_field.`value`,
        good_extended_field.extended_field_id,
        good_extended_field.good_id
    FROM good_extended_field where id= #{id,jdbcType=INTEGER};
    </select>

    <select id="findAllByGoodId" resultMap="goodExtendedFieldResultMap">
        SELECT good_extended_field.good_id,
            good_extended_field.extended_field_id,
            good_extended_field.`value`,
            good_extended_field.id
        FROM good_extended_field where good_id = #{GoodId,jdbcType=INTEGER};
    </select>

    <insert id="save" parameterType="com.example.demo.entity.GoodExtendedField">
        insert into good_extended_field (value, extended_field_id, good_id)
        values (#{value,jdbcType=VARCHAR},#{extendedFieldId,jdbcType=INTEGER},#{goodId,jdbcType=INTEGER})
    </insert>

</mapper>